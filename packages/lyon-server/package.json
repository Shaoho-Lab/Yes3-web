{
  "name": "lyon-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "yarn build && node -r dotenv/config dist"
  },
  "dependencies": {
    "firebase": "^9.10.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.16",
    "rimraf": "^3.0.2"
  }
}
